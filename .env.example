# MongoDB Connection String (replace with your MongoDB Atlas connection string)
MONGO_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/shyam_furnitures?retryWrites=true&w=majority

# JWT Secret for authentication
JWT_SECRET=your_jwt_secret_key_here

# Node Environment
NODE_ENV=production

# Port (Vercel will use its own port)
PORT=5000

# Auth Bypass (set to false in production)
BYPASS_AUTH=false
