{
  "name": "shyam-furnitures",
  "version": "1.0.0",
  "description": "Furniture shop website for Shyam Furnitures",
  "main": "index.js",
  "scripts": {
    "start": "node server.js",
    "server": "nodemon server/index.js",
    "client": "cd client && npm run dev",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "data:import": "node server/utils/seeder",
    "data:destroy": "node server/utils/seeder -d",
    "install-all": "npm install && cd client && npm install && cd ../server && npm install",
    "build": "cd client && npm install --include=dev && npm run build",
    "vercel-build": "npm install && cd client && npm install --include=dev && npm run build",
    "render-build": "npm install && cd server && npm install && cd .. && cd client && npm install --include=dev && npm run build && cd ..",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "furniture",
    "ecommerce",
    "mern",
    "react",
    "node",
    "express",
    "mongodb"
  ],
  "author": "Shyam Furnitures",
  "license": "MIT",
  "dependencies": {
    "concurrently": "^8.0.1",
    "express": "^4.21.2",
    "node-fetch": "^2.7.0",
    "path": "^0.12.7",
    "mongoose": "^8.0.0",
    "dotenv": "^16.0.0",
    "cors": "^2.8.5",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.0",
    "cloudinary": "^1.37.0",
    "multer": "^1.4.5-lts.1"
  }
}
